server:
  port: ${AGENT_APP_PORT:8080}

spring:
  application:
    name: industrial-agent-app
  profiles:
    default: pgvector
  ai:
    model:
      chat: openai
      embedding: openai
    openai:
      base-url: ${ZHIPU_BASE_URL:https://open.bigmodel.cn/api/paas/v4}
      api-key: ${ZHIPU_API_KEY:540d25421eba4ee3b20043e1b2237dc4.nTOxv8wOv1ChtODi}
      chat:
        model: ${ZHIPU_CHAT_MODEL:glm-4-flash}
        temperature: 0.1
        timeout: ${ZHIPU_CHAT_TIMEOUT:180s}
      embedding:
        model: ${ZHIPU_EMBEDDING_MODEL:embedding-3}
        dimensions: ${ZHIPU_EMBEDDING_DIMENSIONS:1024}
    mcp:
      client:
        enabled: true
        name: industrial-agent-mcp-client
        version: 0.1.0
        type: SYNC
        initialized: true
        request-timeout: 20s
        toolcallback:
          enabled: true
        streamable-http:
          connections:
            iiot:
              url: ${IIOT_MCP_BASE_URL:${MCP_SERVER_BASE_URL:http://localhost:8081}}
              endpoint: /mcp

industrial-ai:
  knowledge:
    upload-dir: ${KNOWLEDGE_UPLOAD_DIR:./data/knowledge}
    chunk-size: ${KNOWLEDGE_CHUNK_SIZE:1200}
    chunk-overlap: ${KNOWLEDGE_CHUNK_OVERLAP:200}
  rag:
    top-k: ${RAG_TOP_K:4}
    similarity-threshold: ${RAG_SIMILARITY_THRESHOLD:0.25}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  config: classpath:log4j2-spring.xml
